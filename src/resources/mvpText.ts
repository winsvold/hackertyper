import { createTextFragment, TextFragment } from "../modules/selfWritingConsole/textFragment";

export const mvpText: TextFragment[] = [
  createTextFragment("MVP", 100, 2000),
  createTextFragment(" - Minimum viable productions", 60, 1500),
  createTextFragment(" presents", 100, 500),
  createTextFragment("..", 200, 500),
];

export default mvpText;
